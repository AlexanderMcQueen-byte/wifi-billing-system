# WiFi Billing System

Minimal scaffold for a WiFi Billing System backend (Flask).

## Quick start

Create a virtual environment and install dependencies:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run the app:

```bash
python run.py
```

Run tests:

```bash
pytest
```
